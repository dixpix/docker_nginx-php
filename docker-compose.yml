services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - "./:/var/www/html"
    ports:
      - "80:80"
      - "3000:80"
    healthcheck:
      test: ["CMD", "pgrep", "nginx"]
      interval: 15s
      timeout: 10s
      retries: 3
      start_period: 15s

volumes:
  docker: null
