services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - "./:/var/www/html"
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD", "service", "nginx", "status", "&&", "pgrep", "php-fpm"]
      interval: 15s
      timeout: 10s
      retries: 3
      start_period: 15s

volumes:
  docker: null
